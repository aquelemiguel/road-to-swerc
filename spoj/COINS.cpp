#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0); // Input and output become more efficient.
    cin.tie();

    int coin;
    while (scanf("%d", &coin) != EOF) {
        int exchange[] = {int(floor(coin/2.0)), int(floor(coin/3.0)), int(floor(coin/4.0))};
    }

}